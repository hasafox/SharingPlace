<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_updata"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.hax.sharingplace.UpdateActivity">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/udt_icon"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:src="@drawable/icon_image"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />


    <EditText
        android:id="@+id/udt_email"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:hint="@string/email"
        android:layout_below="@id/udt_icon"
        android:inputType="textEmailAddress"
        android:layout_marginTop="15dp"/>

    <Button
        android:id="@+id/udt_submit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/send"
        android:textAllCaps="false"
        android:textColor="#ffffff"
        android:background="#0066cc"
        android:layout_below="@id/udt_email"
        android:layout_marginTop="10dp"/>

</RelativeLayout>
